<div class="product-rings" aria-hidden="true">
  <div class="grid-bg"></div>
  <div class="ring ring-1"></div>
  <div class="ring ring-2"></div>
  <div class="ring ring-3"></div>
  <div class="ring-center"></div>
</div>

<style>
  .product-rings {
    position: relative;
    height: 360px;
    overflow: hidden;
  }

  .grid-bg {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(to right, rgba(255, 255, 255, 0.05) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
    background-size: 45px 45px;
    -webkit-mask-image: radial-gradient(75% 70% at 55% 50%, #000 10%, transparent 100%);
    mask-image: radial-gradient(75% 70% at 55% 50%, #000 10%, transparent 100%);
  }

  .ring {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: rotate(45deg);
  }

  .ring-1 {
    width: 280px;
    height: 280px;
    margin: -140px 0 0 -140px;
    border: 1px solid #232323;
    animation: dexicSpin 120s linear infinite;
  }

  .ring-2 {
    width: 176px;
    height: 176px;
    margin: -88px 0 0 -88px;
    border: 1px solid #2e2e2e;
    animation: dexicSpinBack 84s linear infinite;
  }

  .ring-3 {
    width: 80px;
    height: 80px;
    margin: -40px 0 0 -40px;
    border: 1px solid #3a3a3a;
    animation: dexicSpin 56s linear infinite;
  }

  .ring-center {
    position: absolute;
    left: 50%;
    top: 50%;
    width: 14px;
    height: 14px;
    margin: -7px 0 0 -7px;
    background: var(--accent);
    transform: rotate(45deg);
    animation: dexicPulse 5s ease-in-out infinite;
  }

  @media (max-width: 900px) {
    .product-rings {
      display: none;
    }
  }
</style>
